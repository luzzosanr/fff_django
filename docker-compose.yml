version: '3.7'

services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: sh -c "python manage.py runserver 0.0.0.0:8000"
    env_file:
      - .env
    environment:
      - ALLOWED_HOSTS=127.0.0.1,localhost
      - DEBUG=1